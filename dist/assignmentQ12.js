"use strict";
/* Greetings: Start with the array you used in Exercise 11, but instead of just printing each person’s name,
print a message to them. The text of each message should be the same, but each message should be personalized with the person’s
 name. */
//  import { frndArr } from "./assignmentQ11";
let frndArr = ['hammad', 'qasim', 'dawood', 'shaqas'];
let greetMsg = "you are invited on Birthday Party!!!!";
for (let frndMsg of frndArr) {
    console.log("🚀 ~ file: assignmentQ12.ts:8 ~ frndArr:", frndMsg, `${greetMsg}`);
}
